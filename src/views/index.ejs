<!DOCTYPE html>
<html lang="es">
    
    <!--Head-->
    <%-include('./partials/head.ejs')%>
    <link rel="stylesheet" href="css/style.css"> <!--Editar ruta de archivo-->
</head>
    <!--Hasta acá-->

<body>
    <!--Header-->
    <%-include('./partials/header.ejs')%>
    <!--Carousel robado de internet, falta darle dinamismo con EJS-->
    <div class="carousel">
        <div class="slides">
            <div class="slide" id="slide-1">
                <img src="/images/carousel1/banner.jpg" alt="Slide 1">
            </div>
            <div class="slide" id="slide-2">
                <img src="/images/carousel1/banner1.jpg" alt="Slide 2">
            </div>
            <div class="slide" id="slide-3">
                <img src="/images/carousel1/banner2.jpg" alt="Slide 3">
            </div>
            <div class="slide" id="slide-4">
                <img src="/images/carousel1/banner1.jpg" alt="Banner">
            </div>
        </div>
    
        <div class="controls">
            <label for="slide1"></label>
            <label for="slide2"></label>
            <label for="slide3"></label>
            <label for="slide4"></label>
        </div>
    </div>

    <!--Comienzo del main y sus productos-->
    <!--Se accede al json a través del controlador, se convierten los productos en objetos y se manipulan acá-->
    <main>
        <section>
            <h1>Ofertas</h1>
            <div class="carousel-productos">


            <% productos.forEach(product => { %>
            <article class="article">
                    <a href="/product/detail/<%=product.id%>"><div class="product-card">
                    <img src="<%=product.img%>" alt="">
                <div class="product-detail">
                <p class="discount"><%= product.descuento%>% Off</p>
                <p class="precio">$<%= product.precio%></p>
                <p class="marca"><%= product.marca%></p>
                <h3><a href="/product/detail"><%= product.nombre%></a></h3>
                <div class="reputation-icon">
                    <% for(let x = 1 ; x <= product.punt ; x++){ %>
                    <i class="fa-solid fa-star"></i>
                    <% } %>
                </div>  
                </div>
                </div></a>
            </article>
            <% }) %>
        </div>
        </section>

        <!--Falta modificar estilo, añadí esta section porque se veía vacío el index después de poner todos los productos en carousel-->
        <section>
            <h1>Te puede interesar</h1>
            <div class="carousel-productos">


                <% productos.forEach(product => { %>
                <article class="article">
                        <a href="/product/detail/<%=product.id%>"><div class="product-card">
                        <img src="<%=product.img%>" alt="">
                    <div class="product-detail">
                    <p class="precio">$<%= product.precio%></p>
                    <p class="marca"><%= product.marca%></p>
                    <h3><a href="/product/detail"><%= product.nombre%></a></h3>
                    <div class="reputation-icon">
                        <% for(let x = 1 ; x <= product.punt ; x++){ %>
                        <i class="fa-solid fa-star"></i>
                        <% } %>
                    </div>  
                    </div>
                    </div></a>
                </article>
                <% }) %>
            </div>
        </section>

        <!--El historial lo dejé en un contenedor flex en el que por el momento muestra máximo 4 productos-->
        <section>
            <h1>Historial</h1>
            <section>
                <div class="container-flex">
                <% productos.forEach(product => { %>
                <article class="article">
                    <a href="/product/detail/<%=product.id%>"><div class="product-card">
                        <img src="<%=product.img%>" alt="">
                    <div class="product-detail">
                    <p class="precio">$<%= product.precio%></p>
                    <p class="marca"><%= product.marca%></p>
                    <h3><a href="/product/detail"><%= product.nombre%></a></h3>
                    <div class="reputation-icon">
                        <% for(let x = 1 ; x <= product.punt ; x++){ %>
                        <i class="fa-solid fa-star"></i>
                        <% } %>
                    </div>  
                    </div>
                    </div></a>
                </article>
                <% }) %>
            </div>
        </main>

        <!--Esta section también tiene su json a parte con marcas populares y es dinámica-->
        </section>
        <section class="categorias-populares">
            <h1>Marcas populares</h1>
            <div class="container-flex">
            <% marcas.forEach(marca => { %>
            <article>
                <div class="product-card">
                    <img src="<%= marca.img %>" alt="">
                </div>
            </article>
            <% }) %>
        </section>
    
    <!--Footer-->  
    <%-include('./partials/footer.ejs')%>
</body>
</html>